#include<stdio.h>
int main()
{
    int p1,u1;
    int p2,u2;
    double p1price,p2price;
    double total;
    scanf("%d %d %lf",&p1,&u1,&p1price);
    scanf("%d %d %lf",&p2,&u2,&p2price);
    total=(u1*p1price)+(u2*p2price);
    printf("VALOR A PAGAR: R$ %.2lf\n",total);
    return 0;
}
