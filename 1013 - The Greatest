#include<stdio.h>
#include<stdlib.h>
int main()
{
    int a,b,c,max,maxf;
    scanf("%d %d %d",&a,&b,&c);
    maxf=((a+b+abs(a-b))/2);
    max=((maxf+c+abs(maxf-c))/2);
    printf("%d eh o maior\n", max);
    return 0;
}


